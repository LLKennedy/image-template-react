import React from 'react';

export default class ImageTemplate extends React.Component {
    constructor(props) {
        super(props);
        this.state = {
            
        };
    }

    render() {
        return (
            <canvas/>
        )
    }
}